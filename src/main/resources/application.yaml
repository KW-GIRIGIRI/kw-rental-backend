spring:
    jpa:
        properties:
            hibernate:
                show_sql: true
                format_sql: true
                use_sql_comments: true
    config:
        import: security/security-default.yml

server:
    shutdown: graceful
    servlet:
        session:
            cookie:
                same-site: none
                secure: true